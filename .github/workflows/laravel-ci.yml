name: Laravel CI

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  laravel-tests:
    runs-on: ubuntu-latest

    services:
      mysql:
        image: mysql:8.0
        env:
          MYSQL_DATABASE: laravel
          MYSQL_USER: laravel
          MYSQL_PASSWORD: laravel
          MYSQL_ROOT_PASSWORD: root
        ports:
          - 3306:3306
        options: >-
          --health-cmd="mysqladmin ping -h localhost"
          --health-interval=10s
          --health-timeout=5s
          --health-retries=3

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up PHP
        uses: shivammathur/setup-php@v2
        with:
          php-version: '8.2'
          extensions: mbstring, pdo, pdo_mysql, bcmath, intl
          coverage: none

      - name: Install Composer dependencies
        run: cd src && composer install --prefer-dist --no-progress

      - name: Copy .env
        run: cd src && cp .env.example .env

      - name: Generate application key
        run: cd src && php artisan key:generate

      - name: Run database migrations
        run: cd src && php artisan migrate --force

      - name: Run Unit Tests
        run: cd src && php artisan test --testsuite=Unit

      - name: Run Feature Tests
        run: cd src && php artisan test --testsuite=Feature
